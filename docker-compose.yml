version: '3'
services:
  backend:
    build: .
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"

  frontend:
    build: .
    command: python frontend/app.py
    ports:
      - "5000:5000"
    depends_on:
      - backend